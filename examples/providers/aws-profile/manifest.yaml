version: "1"
name: aws-profile
package_version: "0.1.0"
description: AWS CLI por aliases humanos de profile com verificação de conta.
provider: provider.yaml
rules: rules.yaml
environment: env.example
setups:
  - name: readonly
    kind: readonly
    description: Descoberta e leitura operacional comum, sem operações de escrita.
    rules: setups/readonly/rules.yaml
