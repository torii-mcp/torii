# Evidência live — follow-up de lifecycle e UX — 2026-07-15

Status da sessão: `CLOSED`

Esta sessão valida o binário posterior à implementação do lifecycle herdado por target e às correções `UX-001` e `UX-002`. Identificadores reais de cliente não entram nesta evidência.

## Resumo

| Instância | Caso | Resultado | Observação |
|---|---|---|---|
| LIVE-2026-07-15-F01 | UX-001 / AWS-AUTH-01 | FAIL | altura melhor, mas ainda com espaço vertical excessivo; cancelamento funcional passou |
| LIVE-2026-07-15-F02 | UX-001 / AWS-AUTH-01 | PASS | operador confirmou melhora substancial; solicitou ícone e rodapé alinhado como refinamentos |
| LIVE-2026-07-15-F03 | UX-001 / AWS-AUTH-01 | PASS | operador aprovou o ícone inicial, o rodapé ancorado e o fluxo de campos vazios |
| LIVE-2026-07-15-F04 | UX-002 / AWS-AUTH-03 | FAIL | spinner aprovado; token longo expandiu o campo e desestabilizou o formulário |
| LIVE-2026-07-15-F04-R1 | UX-002 / AWS-AUTH-03 | FAIL | scrollbar passou; clipboard sem margem e status acima dos botões ainda exigem refinamento |
| LIVE-2026-07-15-F04-R2 | UX-002 / AWS-AUTH-03 | FAIL | status fixo passou; barra ficou recuada lateralmente e botões permaneceram à esquerda |
| LIVE-2026-07-15-F04-R3 | UX-002 / AWS-AUTH-03 | FAIL | barra aprovada; painel de ações expandiu e cobriu o formulário durante o repaint |
| LIVE-2026-07-15-F04-R4 | UX-002 / AWS-AUTH-03 | PASS | layout final, spinner, erro coral e cancelamento aprovados |
| LIVE-2026-07-15-F05 | UX-002 / AWS-AUTH-04 | PASS | candidata validada, spinner aprovado e operação read-only concluída |
| LIVE-2026-07-15-F05-R1 | UX-002 / AWS-AUTH-04 | PASS | joinha, status verde e fechamento automático aprovados |
| LIVE-2026-07-15-F06 | AUTH-UI-01 | PASS | layout de um campo aprovado; espaço livre considerado aceitável |
| LIVE-2026-07-15-F07 | AUTH-UI-02 | PASS | quatro campos simples aprovados; solicitado retry com C e D multilinha |
| LIVE-2026-07-15-F07-R1 | AUTH-UI-02 | PASS | quatro campos com C e D multilinha aprovados |
| LIVE-2026-07-15-F08 | AUTHZ-UI-01 | PASS | layout normal, estados e feedback da negação aprovados |
| LIVE-2026-07-15-F08-R1 | AUTHZ-UI-01 | PASS | comando de 4 KiB contido; solicitado ancorar confirmações no fundo |
| LIVE-2026-07-15-F08-R2 | AUTHZ-UI-01 | FAIL | confirmações permaneceram na posição anterior |
| LIVE-2026-07-15-F08-R3 | AUTHZ-UI-01 | FAIL | estrutura aprovada; espaçamento superior do painel desigual |
| LIVE-2026-07-15-F08-R4 | AUTHZ-UI-01 | PASS | layout final da autorização aprovado pelo operador |
| LIVE-2026-07-15-F09 | AUTHZ-UI-02 | PASS | feedback verde e transição para autenticação aprovados |
| LIVE-2026-07-15-F10 | AUTHZ-UI-03 | PASS | feedback verde com duração e transição aprovados |
| — | K8S-POL-01 | NOT RUN | adiado para uma sessão específica de targets e lifecycle |

## LIVE-2026-07-15-F01 — UX-001 / AWS-AUTH-01

- caso: dimensionamento da janela e validação de campos obrigatórios vazios;
- início do preparo: `2026-07-15T16:43:15-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-001-20260715-followup-001`;
- binário Torii SHA-256: `9ec21c5b2b346f5d85e7a656b6ccd1f82c3f198a1206329b20951090a5ab3599`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache ou grants;
- resultado: `FAIL`;

Rules aprovadas, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: confirmar que a janela está compacta, clicar **Validar e usar** com os três campos vazios, confirmar a mensagem local de campos obrigatórios e clicar **Cancelar**.

Esperado: nenhuma janela de autorização; `invoke`, `allowed-by-rules` e `session-invalid`; nenhuma validação remota, persistência, grant ou execução da operação solicitada.

Resultados observados:

- confirmação visual do operador: a janela ficou menor que a versão anterior, mas ainda apresentou uma região vertical vazia grande abaixo dos botões;
- critério `UX-001`: `FAIL`;
- campos vazios: a validação local impediu submissão ao validator;
- cancelamento: retornou erro estruturado `AuthCancelled`;
- eventos de auditoria, em ordem: `invoke`, `allowed-by-rules`, `session-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- conclusão sobre travamento: não houve processo travado; o resultado da sessão de execução ainda não havia sido coletado quando o operador perguntou;
- follow-up solicitado: adotar o padrão do awsgate, com validator em background dentro do subprocesso da GUI, e reduzir novamente a altura;
- fim da validação: `2026-07-15T16:47:40-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F02 — UX-001 / AWS-AUTH-01

- caso: segunda validação do dimensionamento da janela;
- início: `2026-07-15T16:53:46-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-001-20260715-followup-002`;
- binário Torii SHA-256: `a2934f82bba5e440fa78980b71adea668df9365230ae5c1eb881c6ca328c03ff`;
- provider AWS: `0.1.1`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache ou grants;
- resultado: `PASS`.

Rules aprovadas, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana: confirmar a nova altura, clicar **Validar e usar** com os campos vazios, confirmar a mensagem local e clicar **Cancelar**.

Resultados observados:

- operador confirmou que o novo tamanho melhorou substancialmente e que o fluxo funcionou;
- refinamentos solicitados sem invalidar o resultado: adicionar um ícone de torii com cordas/nós japoneses e alinhar separator/botões ao rodapé para absorver a pequena sobra de altura;
- cancelamento: retornou erro estruturado `AuthCancelled`;
- eventos de auditoria, em ordem: `invoke`, `allowed-by-rules`, `session-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos remanescentes: zero;
- fim: `2026-07-15T16:56:35-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F03 — UX-001 / AWS-AUTH-01

- caso: homologação visual do ícone inicial e do separator/botões ancorados no rodapé;
- início do preparo: `2026-07-15T17:10:30-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-001-20260715-followup-003`;
- binário Torii SHA-256: `342ea604c0794c76f7ed875cba3e41d06f71cce49e27e4eac8b587d534e1864d`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- rules aprovados pelo operador antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: confirmar o ícone nativo, verificar que separator e botões permanecem no rodapé, clicar **Validar e usar** com os campos vazios, confirmar a mensagem local e clicar **Cancelar**.

Esperado: nenhuma janela de autorização; nenhuma validação remota, persistência, grant ou execução da operação solicitada; cleanup integral da raiz isolada.

Resultados observados:

- operador confirmou visualmente o ícone inicial e o rodapé ancorado;
- nenhuma janela de autorização apareceu;
- a validação local dos campos vazios exibiu a mensagem vermelha esperada;
- cancelamento: retornou erro estruturado `AuthCancelled`;
- eventos de auditoria, em ordem: `invoke`, `allowed-by-rules`, `session-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T17:13:04-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F04 — UX-002 / AWS-AUTH-03

- caso: manter a janela responsiva e visível durante a validação de uma candidata inválida;
- início do preparo: `2026-07-15T17:16:05-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-004`;
- binário Torii SHA-256: `32090b98fe40287fe5f9af51a0ceb63f5634a2bba21d61d8aa18a039bacc3143`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- rules aprovados pelo operador antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: preencher os três campos com valores temporários deliberadamente inválidos, clicar **Validar e usar**, observar spinner/cursor de espera com a mesma janela aberta e, depois da recusa, confirmar o erro na própria janela e clicar **Cancelar**.

Esperado: nenhuma janela de autorização; formulário e submit desabilitados enquanto o validator roda em background; nenhuma persistência, grant ou execução da operação solicitada; cleanup integral.

Resultados observados:

- operador confirmou que o spinner ficou visível e adequado;
- falha visual: o session token longo expandiu o campo, ativou rolagem do formulário e ocultou o Access key ID acima;
- cinco tentativas de candidata inválida foram auditadas, sem registrar os valores fornecidos;
- eventos iniciais: `invoke`, `allowed-by-rules`, `session-invalid`;
- eventos posteriores: cinco `session-candidate-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- correção solicitada: altura fixa para o campo multilinha e scrollbar interna visível quando necessária;
- fim: `2026-07-15T17:21:18-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F04-R1 — UX-002 / AWS-AUTH-03

- caso: retry do F04 com altura fixa e scrollbar interna no campo multiline;
- início do preparo: `2026-07-15T17:27:27-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-004-r1`;
- binário Torii SHA-256: `342ea604c0794c76f7ed875cba3e41d06f71cce49e27e4eac8b587d534e1864d`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- rules aprovados pelo operador antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: preencher os três campos com valores temporários deliberadamente inválidos, usando um session token longo, clicar **Validar e usar**, confirmar que somente o campo do token recebe scrollbar, que Access key ID e Secret access key permanecem visíveis e que a barra de status alterna entre progresso e erro sem deslocar o formulário; depois da recusa, clicar **Cancelar**.

Esperado: spinner funcional na barra de status; erro na mesma barra; altura do campo multiline limitada a `72 px`; botões na última linha do rodapé; nenhuma rolagem do formulário AWS; nenhuma persistência, grant ou execução solicitada; cleanup integral.

Resultados observados:

- altura fixa e scrollbar interna do session token: aprovadas;
- spinner funcional: aprovado;
- divergência visual 1: botão de clipboard encostado no campo multiline;
- divergência visual 2: status acima dos botões, sem tratamento visual de barra do sistema, e variação de altura percebida ao entrar o spinner;
- refinamento solicitado: `1 px` acima do clipboard; botões antes da barra; barra escura como último elemento; altura fixa que comporte spinner sem deslocamento;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`, `session-candidate-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T17:34:26-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F04-R2 — UX-002 / AWS-AUTH-03

- caso: segundo retry do F04 com margem do clipboard e barra de status inferior fixa;
- início do preparo: `2026-07-15T17:37:28-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-004-r2`;
- binário Torii SHA-256: `aefabdcca965410dfaebe04eb99eb8373a9f699434619a8df76c7ff9748c1d5b`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- rules aprovados pelo operador antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: usar valores inválidos e token longo; confirmar `1 px` acima do clipboard, botões acima da barra escura, barra como último elemento e altura invariável ao alternar entre pronto, spinner e erro; depois clicar **Cancelar**.

Esperado: scrollbar interna aprovada no R1 permanece; barra escura de `24 px`; spinner de `16 px`; nenhum deslocamento do formulário ou do footer; nenhuma persistência, grant ou `ran`; cleanup integral.

Resultados observados:

- margem acima do clipboard, scrollbar interna, barra escura inferior e altura fixa: aprovadas;
- divergência visual 1: barra herdou margem horizontal e não alcançou as bordas da janela;
- divergência visual 2: botões permaneceram alinhados à esquerda;
- refinamento solicitado: barra em painel inferior independente, edge-to-edge; botões alinhados à direita com a ação principal no extremo direito;
- duas tentativas de candidata inválida foram auditadas, sem registrar os valores fornecidos;
- eventos iniciais: `invoke`, `allowed-by-rules`, `session-invalid`;
- eventos posteriores: dois `session-candidate-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T17:41:34-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F04-R3 — UX-002 / AWS-AUTH-03

- caso: terceiro retry do F04 com barra inferior edge-to-edge e ações alinhadas à direita;
- início do preparo: `2026-07-15T17:45:27-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-004-r3`;
- binário Torii SHA-256: `12c4b8493a9b1fd29b1d0d994913c226eb3d7066a29684e6559482f18ed48361`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- rules aprovados pelo operador antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: confirmar que a barra escura alcança as bordas esquerda, direita e inferior; que **Validar e usar** ocupa o extremo direito e **Cancelar** fica imediatamente à esquerda; usar valores inválidos e token longo para rever os estados pronto, spinner e erro; depois clicar **Cancelar**.

Esperado: todos os critérios aprovados em R1/R2 permanecem; barra e ações atendem ao novo alinhamento; nenhuma persistência, grant ou `ran`; cleanup integral.

Resultados observados:

- barra inferior edge-to-edge: aprovada;
- alinhamento inicial dos botões à direita: aprovado;
- falha visual: a altura automática do painel de ações aumentou durante o repaint e o painel passou a cobrir o formulário até o topo;
- correção solicitada: altura exata também para o painel de ações, com centralização vertical e sem espaçadores na medição;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T17:48:04-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F04-R4 — UX-002 / AWS-AUTH-03

- caso: quarto retry do F04 com altura exata para o painel de ações;
- início do preparo: `2026-07-15T17:50:59-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-004-r4`;
- binário Torii SHA-256: `a3bac4f8e5c85e7f00db199d96e558eb0fd45dfb4b2e9751151511b6d27d4083`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- processo de iteração: build rápido aprovado pelo operador para mudança somente visual; suíte completa adiada até o fechamento da janela;
- rules inalterados em relação ao R3 e reapresentados antes do preparo.

Rules, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: confirmar que o painel de ações permanece com `32 px`, sem crescer ou cobrir o formulário durante repaints; rever barra edge-to-edge e botões à direita; provocar uma candidata inválida e conferir o erro em coral dessaturado `#E06C75`; clicar **Cancelar**.

Esperado: janela estável desde o primeiro frame; nenhum validator, persistência, grant ou `ran`; cleanup integral.

Resultados observados:

- painel de ações permaneceu com altura fixa, sem cobrir o formulário;
- botões à direita, barra edge-to-edge, scrollbar interna, spinner estável e erro coral: aprovados pelo operador;
- cancelamento: retornou erro estruturado `AuthCancelled` e encerrou corretamente o subprocesso;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`, `session-candidate-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes depois da chamada;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T17:53:36-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F05 — UX-002 / AWS-AUTH-04

- caso: observar o spinner e concluir autenticação com uma candidata válida;
- início do preparo: `2026-07-15T17:54:53-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-005`;
- binário Torii SHA-256: `a3bac4f8e5c85e7f00db199d96e558eb0fd45dfb4b2e9751151511b6d27d4083`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- dependência: F04-R4 encerrado; rules serão apresentados novamente antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: preencher os três campos com uma credencial temporária válida somente na GUI, clicar **Validar e usar** e observar spinner/cursor de espera com a mesma janela aberta até a validação terminar.

Esperado: nenhuma janela de autorização; candidata persistida somente depois de validada; operação solicitada executada uma vez; nenhuma credencial, cache ou grant permanece depois do cleanup integral.

Resultados observados:

- operador forneceu a candidata válida somente na GUI e confirmou o spinner;
- validator read-only: sucesso; janela fechou automaticamente;
- operação solicitada: exit code zero, stdout presente e stderr ausente; identidade e stdout não foram retidos na evidência;
- eventos, em ordem: `invoke`, `allowed-by-rules`, `session-invalid`, `session-refreshed`, `ran`;
- `ran`: um;
- `auth/credentials.env`: presente somente antes do cleanup, tamanho `1143` bytes e SHA-256 `65d9e18b75305a68f0581c6f082943d9a0e91eca7b26ab7c02c165deeed55071`;
- `providers/aws/.session-cache`: presente somente antes do cleanup, tamanho `10` bytes e SHA-256 `0bfd2bf76b6942c95ee831e33c27314ec658b4e65b8f0a1857e581f2bd544e6b`;
- grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- refinamento posterior sem invalidar o resultado: mostrar brevemente um status verde de sucesso antes do fechamento automático;
- fim: `2026-07-15T17:58:56-03:00`;
- cleanup: `PASS`; a raiz isolada e a credencial temporária não existem mais.

## LIVE-2026-07-15-F05-R1 — UX-002 / AWS-AUTH-04

- caso: retry visual do sucesso com status verde antes do fechamento automático;
- início do preparo: `2026-07-15T18:01:20-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-UX-002-20260715-followup-005-r1`;
- binário Torii SHA-256: `cc31bc6476a7f31b6b8e96cb4ab25f0eeb0d5731623178395524a259fa4e15b1`;
- provider AWS: `0.1.1`;
- AWS CLI: `2.31.13`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- processo de iteração: build rápido, sem suíte intermediária, conforme aprovado pelo operador.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: fornecer uma candidata válida somente na GUI, clicar **Validar e usar**, observar spinner e depois `👍 Sessão validada.` em verde suave por aproximadamente `650 ms`; aguardar o fechamento automático.

Esperado: nenhuma transição intermediária para `Pronto para validar.`; controles desabilitados durante o sucesso terminal; validator e operação read-only com exit code zero; cleanup integral.

Resultados observados:

- operador aprovou a sequência spinner, `👍 Sessão validada.` em verde e fechamento automático;
- joinha renderizou corretamente e o intervalo foi suficiente para leitura;
- validator e operação solicitada: sucesso; exit code zero; stdout presente e não retido na evidência;
- eventos, em ordem: `invoke`, `allowed-by-rules`, `session-invalid`, `session-refreshed`, `ran`;
- `ran`: um;
- `auth/credentials.env`: presente somente antes do cleanup, tamanho `1143` bytes e SHA-256 `65d9e18b75305a68f0581c6f082943d9a0e91eca7b26ab7c02c165deeed55071`;
- `providers/aws/.session-cache`: presente somente antes do cleanup, tamanho `10` bytes e SHA-256 `9ef1d1cc01354aab4a83a489a2345fa0b36572eba478ccf5a2e54b745315cf5d`;
- grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:03:04-03:00`;
- cleanup: `PASS`; a raiz isolada e a credencial temporária não existem mais.

## LIVE-2026-07-15-F06 — AUTH-UI-01

- caso: layout mínimo da autenticação com uma variável;
- início do preparo: `2026-07-15T18:05:26-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTH-UI-01-20260715-followup-006`;
- binário Torii SHA-256: `cc31bc6476a7f31b6b8e96cb4ab25f0eeb0d5731623178395524a259fa4e15b1`;
- provider de teste: um campo obrigatório `Value`, SHA-256 `45fce74d482a2b858d0feab8ff0c1db7059d26259c926724070e879faaad26f4`;
- operação de referência: `sts get-caller-identity`, estritamente read-only;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- dependência: F05-R1 encerrado; provider e rules mínimos apresentados antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: conferir um único campo, barra de status e botões, depois clicar **Cancelar** sem preencher ou validar.

Resultados observados:

- operador aprovou o layout compacto de um campo;
- espaço livre permaneceu visível, mas foi considerado aceitável;
- clipboard, ações e barra de status permaneceram acessíveis e estáveis;
- cancelamento: encerrou corretamente com `AuthCancelled`;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`;
- validator e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:07:21-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F07 — AUTH-UI-02

- caso: layout da autenticação com quatro variáveis;
- início do preparo: `2026-07-15T18:08:32-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTH-UI-02-20260715-followup-007`;
- binário Torii SHA-256: `cc31bc6476a7f31b6b8e96cb4ab25f0eeb0d5731623178395524a259fa4e15b1`;
- provider de teste: quatro campos obrigatórios, SHA-256 `bb5d4acfa00f846477db12a0eaf6b4a3b94f8f8a1100f39879da34d5fa67461c`;
- operação de referência: `sts get-caller-identity`, estritamente read-only;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- dependência: F06 encerrado; provider e rules mínimos apresentados antes do preparo.

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: conferir os quatro campos, a rolagem do formulário se necessária, barra de status e botões, depois clicar **Cancelar** sem preencher ou validar.

Resultados observados:

- operador aprovou o layout com quatro campos simples;
- campos, clipboard, ações e barra permaneceram acessíveis e estáveis;
- cancelamento: encerrou corretamente com `AuthCancelled`;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`;
- validator e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- follow-up solicitado: repetir com `Value C` e `Value D` como campos multilinha grandes;
- fim: `2026-07-15T18:10:28-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F07-R1 — AUTH-UI-02

- caso: quatro variáveis com `Value C` e `Value D` multilinha;
- início do preparo: `2026-07-15T18:12:07-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTH-UI-02-20260715-followup-007-r1`;
- binário Torii SHA-256: `cc31bc6476a7f31b6b8e96cb4ab25f0eeb0d5731623178395524a259fa4e15b1`;
- provider de teste: quatro campos obrigatórios, dois multilinha, SHA-256 `93f1172f4de82d19ecc6de4a26920699cfaf830a46f307b32cd52687454248ad`;
- operação de referência: `sts get-caller-identity`, estritamente read-only;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;

Rules propostos, SHA-256 `6b82372870e90024ab37209bdf403c118b164f1f89938d196f4ae72919320913`:

```yaml
version: "1.0"
deny: []
accept:
  - "sts get-caller-identity"
```

Ação humana prevista: conferir dois campos simples e dois campos multilinha de altura fixa, preencher C e D com textos falsos longos para confirmar scrollbars independentes e clicar **Cancelar** sem validar.

Esperado: C e D não expandem a janela ao receber conteúdo; cada um tem scrollbar própria; formulário, ações e status permanecem acessíveis; nenhum validator, persistência ou `ran`.

Resultados observados:

- operador aprovou a combinação de A/B simples com C/D multilinha;
- textos falsos longos em C e D permaneceram contidos, com scrollbars independentes e sem deslocar o footer;
- cancelamento: encerrou corretamente com `AuthCancelled`;
- eventos: `invoke`, `allowed-by-rules`, `session-invalid`;
- validator e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:13:40-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F08 — AUTHZ-UI-01

- caso: layout da autorização e feedback terminal da negação;
- início do preparo: `2026-07-15T18:19:34-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-01-20260715-followup-008`;
- binário Torii SHA-256: `e9433ebf73005b69aa99d6bbb9a4fee0d7da2e6a1115a79f4b4ea8ed92023176`;
- provider AWS: `0.1.1`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- processo de iteração: build rápido, sem suíte intermediária, conforme aprovado pelo operador.

Rules aprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: conferir o comando em área de altura fixa, botões alinhados à direita e barra de status; marcar **Estou ciente dos riscos**, ativar **Permitir por**, alterar os minutos e conferir a atualização do status; clicar **Negar** e observar `Acesso negado.` em coral antes do fechamento automático.

Esperado: decisão `unresolved` seguida de negação humana; nenhuma autenticação, execução, credencial, cache ou grant; layout estável durante todas as transições; cleanup integral.

Resultados observados:

- operador aprovou o layout, as mudanças de estado e o feedback terminal da negação;
- resposta MCP: `deny`, origem `human-deny`;
- eventos, em ordem: `invoke`, `denied-interface`;
- autenticação e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:21:21-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F08-R1 — AUTHZ-UI-01

- caso: estabilidade da autorização com comando enorme;
- início do preparo: `2026-07-15T18:23:14-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-01-20260715-followup-008-r1`;
- binário Torii SHA-256: `e9433ebf73005b69aa99d6bbb9a4fee0d7da2e6a1115a79f4b4ea8ed92023176`;
- provider AWS: `0.1.1`;
- operação base: `sts get-caller-identity`, estritamente read-only;
- carga visual: argumento sintético `--query` de `4096` bytes, SHA-256 `6896d9ea3f73a4434f5832bc65714e7d066f177373f36f34dc8a6f735daa41b1`;
- invocações previstas: uma, encerrada por negação humana antes de autenticação ou provider;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- processo de iteração: build rápido, sem suíte intermediária, conforme aprovado pelo operador.

Rules aprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: confirmar que o comando enorme permanece contido e rolável sem deslocar formulário, ações ou status; clicar **Negar**.

Esperado: decisão `unresolved` seguida de negação humana; nenhuma autenticação, execução, credencial, cache ou grant; cleanup integral.

Resultados observados:

- operador aprovou a contenção e rolagem do comando de `4096` bytes;
- janela, ações e status permaneceram estáveis;
- resposta MCP: `deny`, origem `human-deny`;
- eventos, em ordem: `invoke`, `denied-interface`;
- autenticação e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- refinamento solicitado sem invalidar o resultado: ancorar o bloco de checkboxes no fundo da área de conteúdo;
- fim: `2026-07-15T18:26:46-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F08-R2 — AUTHZ-UI-01

- caso: comando enorme com confirmações ancoradas no fundo;
- início do preparo: `2026-07-15T18:28:26-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-01-20260715-followup-008-r2`;
- binário Torii SHA-256: `cc89ea3fc0f0a62a0fa8da961ecf61dc3af0f6b733a2824f08107f064f9654bf`;
- provider AWS: `0.1.1`;
- operação base: `sts get-caller-identity`, estritamente read-only;
- carga visual: argumento sintético `--query` de `4096` bytes, SHA-256 `6896d9ea3f73a4434f5832bc65714e7d066f177373f36f34dc8a6f735daa41b1`;
- invocações previstas: uma, encerrada por negação humana antes de autenticação ou provider;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- aprovação: automática dentro da rodada de UI, limitada ao perfil R0 e à operação read-only acordada.

Rules autoaprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: conferir o bloco de confirmações imediatamente acima das ações, com o comando enorme contido; clicar **Negar**.

Esperado: layout estável; decisão `unresolved` seguida de negação humana; nenhuma autenticação, execução, credencial, cache ou grant; cleanup integral.

Resultados observados:

- o comando enorme permaneceu contido e rolável;
- o operador confirmou que os checkboxes continuaram na posição anterior;
- critério de ancoragem inferior: `FAIL`;
- resposta MCP: `deny`, origem `human-deny`;
- eventos, em ordem: `invoke`, `denied-interface`;
- autenticação e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- correção seguinte: painel inferior próprio para as confirmações e comando ocupando o espaço livre;
- fim: `2026-07-15T18:30:30-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F08-R3 — AUTHZ-UI-01

- caso: comando enorme ocupando o espaço livre e confirmações em painel inferior fixo;
- início do preparo: `2026-07-15T18:31:48-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-01-20260715-followup-008-r3`;
- binário Torii SHA-256: `46869e9a5660089ad9a3583013ca949e516529d9589a060443469a82021cd81f`;
- provider AWS: `0.1.1`;
- operação base: `sts get-caller-identity`, estritamente read-only;
- carga visual: argumento sintético `--query` de `4096` bytes, SHA-256 `6896d9ea3f73a4434f5832bc65714e7d066f177373f36f34dc8a6f735daa41b1`;
- invocações previstas: uma, encerrada por negação humana antes de autenticação ou provider;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `FAIL`;
- aprovação: automática dentro da rodada de UI, limitada ao perfil R0 e à operação read-only acordada.

Rules autoaprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: conferir o comando preenchendo todo o espaço livre e as confirmações ancoradas imediatamente acima das ações; clicar **Negar**.

Esperado: layout estável; decisão `unresolved` seguida de negação humana; nenhuma autenticação, execução, credencial, cache ou grant; cleanup integral.

Resultados observados:

- comando preencheu o espaço livre e permaneceu contido e rolável;
- confirmações ficaram corretamente ancoradas no painel inferior;
- operador aprovou a estrutura como aproximadamente `98%`, mas identificou o primeiro checkbox colado ao separador superior;
- critério de espaçamento vertical simétrico: `FAIL`;
- resposta MCP: `deny`, origem `human-deny`;
- eventos, em ordem: `invoke`, `denied-interface`;
- autenticação e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- correção seguinte: adicionar `4 px` acima do primeiro checkbox;
- fim: `2026-07-15T18:34:27-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F08-R4 — AUTHZ-UI-01

- caso: comando enorme, confirmações no fundo e espaçamento vertical equilibrado;
- início do preparo: `2026-07-15T18:35:01-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-01-20260715-followup-008-r4`;
- binário Torii SHA-256: `81adaa0cf25ea406899cdd88ccf6510364a771bbbf48ca35ebe0906e1ec3a85d`;
- provider AWS: `0.1.1`;
- operação base: `sts get-caller-identity`, estritamente read-only;
- carga visual: argumento sintético `--query` de `4096` bytes, SHA-256 `6896d9ea3f73a4434f5832bc65714e7d066f177373f36f34dc8a6f735daa41b1`;
- invocações previstas: uma, encerrada por negação humana antes de autenticação ou provider;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- aprovação: automática dentro da rodada de UI, limitada ao perfil R0 e à operação read-only acordada.

Rules autoaprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: confirmar distâncias visuais equivalentes acima e abaixo do bloco de confirmações; clicar **Negar**.

Esperado: layout estável; decisão `unresolved` seguida de negação humana; nenhuma autenticação, execução, credencial, cache ou grant; cleanup integral.

Resultados observados:

- operador aprovou integralmente a composição final;
- comando de `4096` bytes ocupou o espaço livre, permaneceu contido e rolável;
- confirmações permaneceram ancoradas e com espaçamento superior/inferior equilibrado;
- ações e status permaneceram fixos;
- resposta MCP: `deny`, origem `human-deny`;
- eventos, em ordem: `invoke`, `denied-interface`;
- autenticação e `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:36:16-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F09 — AUTHZ-UI-02

- caso: feedback terminal verde de permitir uma vez;
- início do preparo: `2026-07-15T18:36:54-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-02-20260715-followup-009`;
- binário Torii SHA-256: `81adaa0cf25ea406899cdd88ccf6510364a771bbbf48ca35ebe0906e1ec3a85d`;
- provider AWS: `0.1.1`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- aprovação: automática dentro da rodada de UI, limitada ao perfil R0 e à operação read-only acordada.

Rules autoaprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: marcar **Estou ciente dos riscos**, manter **Permitir por** desmarcado, clicar **Permitir**, observar `👍 Acesso autorizado uma vez.` em verde e o fechamento automático; quando a autenticação abrir, clicar **Cancelar**.

Esperado: `override-once`, seguido de `session-invalid` e cancelamento da autenticação; nenhum grant, credencial, cache ou `ran`; cleanup integral.

Resultados observados:

- operador aprovou o feedback `👍 Acesso autorizado uma vez.` e a transição para autenticação;
- autenticação cancelada conforme previsto;
- eventos, em ordem: `invoke`, `override-once`, `session-invalid`;
- `ran`: zero;
- credenciais, cache e grants: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:38:36-03:00`;
- cleanup: `PASS`; a raiz isolada não existe mais.

## LIVE-2026-07-15-F10 — AUTHZ-UI-03

- caso: feedback terminal verde de autorização temporária;
- início do preparo: `2026-07-15T18:39:12-03:00`;
- raiz isolada: `C:\Users\paulo\AppData\Local\Temp\torii-live-AUTHZ-UI-03-20260715-followup-010`;
- binário Torii SHA-256: `81adaa0cf25ea406899cdd88ccf6510364a771bbbf48ca35ebe0906e1ec3a85d`;
- provider AWS: `0.1.1`;
- operação solicitada: `sts get-caller-identity`, estritamente read-only;
- invocações previstas: uma;
- estado antes da invocação: sem credenciais, cache, grants ou auditoria;
- resultado: `PASS`;
- aprovação: automática dentro da rodada de UI, limitada ao perfil R0 e à operação read-only acordada.

Rules autoaprovadas, SHA-256 `a9fcb303e907187487420f83932db545f81c40184a85ee179867facc660d8758`:

```yaml
version: "1.0"
deny: []
accept: []
```

Ação humana prevista: marcar **Estou ciente dos riscos**, marcar **Permitir por**, selecionar `2` minutos, clicar **Permitir**, observar `👍 Acesso autorizado por 2 min.` em verde e o fechamento automático; quando a autenticação abrir, clicar **Cancelar**.

Esperado: `override-timed` com grant temporário local, seguido de `session-invalid` e cancelamento da autenticação; nenhuma credencial, cache ou `ran`; grant removido no cleanup integral.

Resultados observados:

- operador aprovou `👍 Acesso autorizado por 2 min.` e a transição para autenticação;
- autenticação cancelada conforme previsto;
- eventos, em ordem: `invoke`, `override-timed`, `session-invalid`;
- grant local: presente somente antes do cleanup, tamanho `35` bytes e SHA-256 `5e293c60af052ffb949998cccf92203aa43b17d0cfb5bd6a7f091701bfde2d2b`;
- `ran`: zero;
- credenciais e cache: ausentes;
- processos `torii` e `mcp_probe` remanescentes: zero;
- fim: `2026-07-15T18:40:21-03:00`;
- cleanup: `PASS`; a raiz isolada e o grant temporário não existem mais.

## Pendência para próxima rodada — apresentação do escopo na autorização

Status: `DEFERRED`

Observação registrada em `2026-07-15`: na janela de autorização, providers com grant derivado curto apresentam um escopo humano compacto, enquanto providers com `grant exact` podem repetir o comando inteiro como `Grant`. Em comandos longos, isso torna ambígua a separação visual entre o comando solicitado e o escopo que será gravado no grant temporário.

Direção para análise amanhã:

- avaliar uma regra declarativa no manifest do provider para definir como apresentar o escopo humano do grant, sem alterar a regra real usada pelo Jasper;
- manter o comportamento da AWS, que apresenta escopo compacto quando o provider deriva o grant dos tokens relevantes;
- tratar providers com `grant exact` de forma mais clara, possivelmente exibindo uma mensagem como "somente o comando exato acima" quando a regra for idêntica ao comando;
- preservar a auditabilidade: a regra real do grant continua local, íntegra e verificável, mas a UI não precisa duplicar visualmente um comando enorme;
- validar com um comando Kubernetes longo e com um caso AWS curto, usando apenas aliases de teste e sem registrar identificadores reais.

## Fechamento da rodada

- encerramento: `2026-07-15T18:45:15-03:00`;
- instâncias live registradas: `20`;
- raízes `torii-live-*` remanescentes no diretório temporário: zero;
- instâncias pendentes: zero;
- varredura de identificadores de cliente, ARNs e números de conta em `docs/` e `tests/`: zero arquivos;
- `cargo fmt --all -- --check`: `PASS`;
- `cargo check --all-targets`: `PASS`;
- `cargo test --all-targets`: `PASS`, `44` testes, zero falhas;
- `cargo clippy --all-targets -- -D warnings`: `PASS`;
- `mdbook build docs`: `PASS`;
- `git diff --check`: `PASS`; somente avisos informativos de conversão futura de LF para CRLF.
